# Model Rescue v8 implementation plan

1. Freeze V8M1 authority and invalidate all legacy v3 expert reuse.
2. Refactor the corrected expert helper only enough to emit a separate V8 schema and namespace; preserve v3 behavior.
3. Add a V8 runner, full-universe qualifier, persistent four-shard controller and focused tests.
4. **Complete:** Run fresh folds 0–19 in a new artifact directory; never overwrite or copy old v3 artifacts.
5. **Complete:** Qualify schemas, checkpoints, prediction-only fields, exact held biological keys, finite means, seed/fold identity and target isolation without scoring.
6. **Complete:** Exact `V8M1_CORRECTED_EXPERT_REBUILD_PASS` opened the complete-score-once V8M2 transition.
7. **Complete / Gate FAIL:** V8M2 found a strong signed mean gain but failed the frozen absolute-delta guardrail.
8. **Closed:** V8M3 and V8 five-seed confirmation are permanently not authorized. A separate V9 amendment may test zero-mean residual magnitude recovery without changing V8.
