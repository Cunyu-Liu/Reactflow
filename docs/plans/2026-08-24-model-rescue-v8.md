# Model Rescue v8 implementation plan

1. Freeze V8M1 authority and invalidate all legacy v3 expert reuse.
2. Refactor the corrected expert helper only enough to emit a separate V8 schema and namespace; preserve v3 behavior.
3. Add a V8 runner, full-universe qualifier, persistent four-shard controller and focused tests.
4. Run fresh folds 0–19 in a new artifact directory; never overwrite or copy old v3 artifacts.
5. Qualify schemas, checkpoints, prediction-only fields, exact held biological keys, finite means, seed/fold identity and target isolation without scoring.
6. Only exact V8M1 pass opens a complete-score-once V8M2 transition.
7. V8M2 compares corrected feature41, B1 and MeanAligned under the frozen hierarchy and Gate.
8. Only exact mean-signal eligibility opens the fixed large residual architecture and subsequent five-seed confirmation.
