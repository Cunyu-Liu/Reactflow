# Model Rescue v9 implementation plan

1. **Complete:** Freeze the V8 terminal result and V9 zero-mean residual hypothesis.
2. **Complete:** Implement one shared residual-head class and exact method-balanced mixture CRPS training for both means.
3. **Complete:** Reuse only corrected V8 fold checkpoints and corrected TIC2A feature41 models/caches.
4. **Complete:** Emit prediction-only distributions with exact mean replay and `E|Delta|`.
5. **Complete:** Add complete-universe merge, score-once target join and mechanical top-journal qualifier.
6. **Complete:** Focused synthetic invariants and two-fold real-data engineering smoke passed without score.
7. **Authorized:** Run the fixed 20-fold screen; only the complete prediction-only merge can be scored.
8. Only a full three-metric top-journal PASS opens five-seed confirmation.

## V9M2 to V9M3 transition checklist

This transition is not authorized until every item below is true. Preparing or
editing this checklist does not open score access.

1. The exact fold-result filenames for folds 0 through 19 exist once each.
2. The persistent V9M2 controller exits without an infrastructure error.
3. `merge_model_rescue_v9.py` writes exact status
   `V9M2_COMPLETE_UNSCORED_MERGE_PASS` and the completion marker exists.
4. No partial score, held target, external outcome, or model-selection result
   was read before the complete merge.
5. Make one focused authority commit that updates the human amendment, machine
   amendment, active contract, decision ledger, and phase-specific contract
   test together:
   - current/runnable phase becomes only `V9M3`;
   - training and candidate training become `false`;
   - complete held-score access becomes `true`;
   - partial-fold score access remains `false`;
   - external outcome access remains `false`;
   - V9M2 records the complete unscored merge artifact;
   - V9M4 remains not authorized and not implemented.
6. Run the focused contract and V9 scorer/qualifier tests from the committed
   V9M3 authority state.
7. In one persistent score controller, run `score_model_rescue_v9.py` exactly
   once against the complete V9 merge, the frozen TIC2A merge, and OpenKnot M2.
8. Run `qualify_model_rescue_v9.py` exactly once. The qualifier intentionally
   returns a nonzero process status for a scientific Gate FAIL after writing a
   valid qualification artifact. The controller must preserve and inspect that
   artifact rather than misclassifying the nonzero status as an engineering
   interruption.
9. Only exact `V9M3_TOP_JOURNAL_SCREEN_PASS` permits implementation review for
   V9M4. Exact FAIL closes V9; it never opens formal seeds or changes the Gate.
