# Model Rescue v9 implementation plan

1. Freeze the V8 terminal result and V9 zero-mean residual hypothesis.
2. Implement one shared residual-head class and exact method-balanced mixture CRPS training for both means.
3. Reuse only corrected V8 fold checkpoints and corrected TIC2A feature41 models/caches.
4. Emit prediction-only distributions with exact mean replay and `E|Delta|`.
5. Add complete-universe merge, score-once target join and mechanical top-journal qualifier.
6. Run focused synthetic invariants, then a two-fold real-data engineering smoke without score.
7. Only exact smoke PASS opens the fixed 20-fold screen; only the complete screen can be scored.
8. Only a full three-metric top-journal PASS opens five-seed confirmation.
