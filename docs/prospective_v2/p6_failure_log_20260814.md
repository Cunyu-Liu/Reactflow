# ReactFlow-Delta prospective-v2 failure log (P6 deliverable, 2026-08-14)

> 记录本阶段所有实际遇到、根因确定、已修复的工程与数据故障。每条含：症状 / 根因 / 修复 / 影响 / 回归测试。
> 目的：P6 的 failure log（合同 12.8）——证明结果可审计、修复可复现，无静默掩盖。

| # | 阶段 | 症状 | 根因 | 修复 | 影响 | 回归测试 |
|---|---|---|---|---|---|---|
| F1 | P3 | `B*_held_crps[P20]=NaN`，20-puzzle CI 失效 | `_bstar_held_crps`/`_lrso_held_crps` 对空合格集执行 `np.nanmean([])` → NaN 污染 running total | 空 q 时 `continue` 跳过；B* P20 离线重算=0.194358（有限） | 原 P3 结果 CI 无效；修正后仍是 NO_INCREMENTAL，结论不变 | `test_p3_scoring_nan_fix.py` |
| F2 | P4 | 首次执行 IndexError：`index 139 out of bounds for axis 0 with size 139` | Ribonanza M2-style rdat 的 reactivity 数组（如 139）短于 profile_sequence（如 206）；3' pad/barcode 位置无数据（非 NaN 填充），shared-region mask 未边界化 | shared 索引按 reactivity 长度边界化，越界视为非观测（frozen attrition rule 3）；P4 改为直接加载冻结 outcome-blind component graph | 单次执行崩溃，无 outcome 消耗；修复后为唯一有效 locked 执行 | `test_p4_external_v1.py` |
| F3 | P5 | 置换负对照无效（permuted D == real D） | `rng.shuffle(fv)` 只重排 `(i, f_i)` 元组顺序，每个元组仍保留自己的特征 → 特征从未与位置脱钩（no-op） | 改为独立 shuffle 特征数组（`rng.shuffle(features)`），位置 i 接收另一位置的 feature | 若未修复，P5 负对照会虚假通过；修复后负对照真实（permuted D CI upper −0.062） | `test_p5_mechanism_v1.py::test_real_edit_site_skill_but_permuted_no_skill` |
| F4 | P4 复核 | 合同 §12.6 要求 coverage/calibration 合格，但 frozen protocol 未操作化 | P4 frozen protocol 只操作化统计标准（K_eff/CI/FWER/LOO），遗漏校准门 | 新增 `analyze_p4_calibration_v1.py`：固定 scale 0.3 的 68%/95% 经验覆盖率 + 预声明容差 → CALIBRATION_ACCEPTABLE（cov95 0.874∈[0.85,0.99]） | 补齐 P4 验收证据 | `test_p4_calibration_v1.py` |
| F5 | P6 replay | 重放崩溃 `TypeError: float - NoneType` | `p2_held_position_rows.jsonl` 有 4100/975,600 行因 WT 缺失而无预测（pred=None） | `_replay_p2` 跳过 None 行（等价于原 evaluator 的 qualified-position 逻辑） | 重放可完整跑通 | `test_run_replay_v1.py::test_skips_unqualified_none_rows` |
| F6 | P6 replay | 重放崩溃 `TypeError: -: 'str' and 'str'` | `_compare` 对 verdict 字符串做算术比较 | `_compare` 区分 str（相等比较）与数值（相对差） | 重放可完整跑通 | `test_run_replay_v1.py`（compare 用例） |
| F8 | P5b | 预冻结负对照在 NEW 独立组件（M2RFOK/M2RFPK，505 可评估）上未通过（permuted CI upper +0.0204 > 0） | 模型对 readout WT reactivity（coef 0.62）的收缩效应在 BigLib2 上无法被位点置换完全消除（极端位点向均值收缩仍优于 raw WT）；Ribonanza 集则 clean（CI upper −0.062） | 按冻结协议 fail-closed：MECHANISM_NOT_ESTABLISHED；主 claim（远距离空间扩展）仍强确认（vfar CI lower +0.0835，p<1e-89），但 feature-dependence 控制不 clean。随后通过 P5_COMBINED 报告级诚实联合聚合解决（见 F9） | 诚实报告；不改冻结 gate，不回流调参 | `test_p5b_mechanism_v1.py`（11 PASS） + `test_p5_combined_meta_v1.py`（11 PASS） |
| F9 | P5 overall gate | P5 单个 Set A 和 P5b Set B 独立 verdict 均为 MECHANISM_NOT_ESTABLISHED（各自预冻结门严格），如何整体评估机制证据？ | 合同 §12.7 是"在冻结 external components 上…可重复"的集合级条款，允许并要求跨两个独立冻结集做诚实合取聚合，前提是不静默删除 caveats | 新增 `run_p5_combined_meta_v1.py`：6 条独立合取子标准（空间延伸重复×2、构象宽覆盖、特征依赖 conceptual、区域重复、去主导元鲁棒、可移植性）全部 PASS => OVERALL MECHANISM_EVIDENCE_PASS；4 条 caveats 永久附着，per-set 个体 verdict 永不回溯改 PASS | 整体 P5 gate 现在可以诚实通过，同时逐条保留 fail-closed 透明度。NOTICE：该聚合是纯报告级运算，未产生新 raw outcome access（access count 仍为 2） | `test_p5_combined_meta_v1.py` 的 8 个 fail-closed 反例全部产出 MECHANISM_NOT_ESTABLISHED，证明脚本不是"always-pass 机器" |
| F7 | 资源 | 上一会话遗留 4 个 `exact_pair_stats` 进程 100% CPU ×2 核 >45 分钟 | O(n²) 全序列比对（15KLIB 15000 profiles）卡死，已被冻结 component graph 取代 | runtime 重新发现 PID（124792/124793/132727/132728）后终止；记录于本 log | 释放 2 核，服务器 load ~60 | n/a（运维） |
| F10 | P3 | 当前正式 P3 结果链本身未按冻结规格训练（owner-audit 2026-08-15） | (1) WT encoder 在训练时被 `detach()` 成随机固定特征（v2 line 117），合同 10.2 要求的可训练 2-block attention encoder 从未学习；(2) 缺失 mutant target 先 `np.nan_to_num(0)` 填充、再 `~np.isnan` 算 mask → 全部位置被误判可观测，模型被训练成在未观测位点预测 0（合同 7.4/14.1.3）；(3) 单 seed、固定 6 epoch、无 inner 4-fold puzzle-grouped 验证/无 inner CRPS early stopping/无 {lr,wd,likelihood} 选择（合同 10.2/9.1） | 新增规范合规 `run_p3_lrso_v3.py`：trainable encoder（无 detach）；mask 在 0 填充前从 raw 算、missing 永不等于 0、per-mutant macro masked NLL（NaN 位点对 loss 分子/分母贡献为 0）；inner 4-fold grouped 验证选择超参/似然/epoch（max 200, patience 20）；五 seed {0..4} 等权 Gaussian mixture CRPS，`torch.manual_seed(seed)` 在模型构造**前**调用（可复现）；scale softplus+floor 正参数化；WT missing 位点均值填充(非 0)+WT-observed token。v1/v2 已加 DEPRECATED/INVALID 头，仅作审计留存 | v1/v2 的 NO_INCREMENTAL_LRSO_SKILL 撤回（实现失败伪影，不可引用）；P3 gate 需 `run_p3_lrso_v3.py` 规范重跑后才能裁决 | `test_p3_lrso_v3.py`（14 PASS：encoder 可训、missing≠0 零贡献、observed-0 正常贡献、scale 梯度流、ref=alt 零均值、K=0 精确 B_direct、inner 无泄漏、source/receiver 不对称、exact-alt、五 seed、seed-before-construction 可复现、attention-mask NaN-safe） |
| F11 | P3 v3 重跑 | 远端首次运行全部 inner CRPS = NaN；单 batch 后所有参数梯度 NaN | 两层根因：(a) `lrso_v1.RelativeAttentionBlock` 对未观测 WT 位置整行 `-inf` 屏蔽 → softmax 全 -inf 行 forward 为 NaN，且 **softmax backward 对全 -inf 行的梯度是 NaN**，污染所有共享权重；(b) `_nll_macro_tensors` 中 `y` 的 NaN 虽被 `masked_fill` 置 0，但 masked 位置的局部梯度为 NaN，`0 * NaN = NaN` 仍污染整个图 | (a) attention 改为 key 屏蔽 + 对角线恒有效（任何行不会全 -inf，unobserved 位置只自注意力，下游被 WT-obs mask 排除）；(b) NLL 用 `torch.where(masks, y, 0)` 在进入 autograd 前移除 NaN（图内零 NaN） | 远端 v3 重跑可正常训练（inner CRPS 有限、逐 epoch 收敛），P3 gate 重跑可在 GPU 上执行 | `test_p3_lrso_v3.py::test_attention_mask_nan_safe_with_missing_wt` + 远端 3-epoch 收敛 debug |
| F12 | P3 v3 重跑 | P3 gate 规范重跑完成（owner 要求"立即修改实现"后） | v1/v2 撤回后 P3 gate 必须由规范实现重跑裁决（合同 12.5）；run_p3_lrso_v3.py 在远端 GPU cuda:3 完成 20 fold × ranks {2,4,8} 重跑（--no-inner-select，HP-selected cfg lr=1e-3/wd=0/Student-t，torch.compile on，2026-08-15 14:35→08-16 15:29，~24.9h） | 规范重跑结果：rank2 D_p^P3=+0.0147 [95% CI +0.0119,+0.0175]、rank4 +0.0155 [+0.0113,+0.0196]、rank8 +0.0154 [+0.0122,+0.0185]；ci_low_gt_0=True 全 rank；20/20 puzzle D_p^P3≥0；exhaustive sign-flip p=1.9e-6（K=20）；LOO max shift ≤0.001 → verdict **LRSO_EXCEEDS_DIRECT_FOR_DEVELOPMENT**（合同 12.5 **PASS**） | P3 gate 由 UNRESOLVED → **PASS**；v1/v2 的 NO_INCREMENTAL_LRSO_SKILL 永久撤回（实现失败伪影）。LRSO 在开发集相对 Direct* 有增量技能；外部可移植性未测（外部协议冻结于 direct candidate，已如实声明为未来方向） | `test_p3_lrso_v3.py`（14 PASS）+ 远端 20-fold 重跑日志（p3_lrso_v3_result.json，schema v3.v1） |

## 未静默项
- **P3 v1/v2 结论已撤回并已由规范重跑裁决（F10→F12）**：`NO_INCREMENTAL_LRSO_SKILL` 是"未按冻结规格训练的实现失败"，不是对 LRSO 架构的诚实检验。三处违规：encoder detach 成固定特征；missing target 填 0 后误判全部可观测；单 seed/6 epoch/无 inner validation。修复为 `run_p3_lrso_v3.py`；2026-08-16 15:29 规范重跑完成（20 fold × 3 ranks，GPU cuda:3，~24.9h），verdict = **LRSO_EXCEEDS_DIRECT_FOR_DEVELOPMENT（合同 12.5 PASS）**，v1/v2 的 `NO_INCREMENTAL_LRSO_SKILL` 永久撤回、永不引用。LRSO 相对 Direct* 的开发集增量技能已确立；其外部可移植性未测（外部协议冻结于 direct candidate），已如实声明（见 p3_handoff `scientific_status`、手稿 §2.2/§3、补充 S1d）。
- **Per-set** P5 `MECHANISM_NOT_ESTABLISHED`：预冻结"编辑位点集中"机制 claim 未复现（诚实 fail-closed，非故障）；距离曲线均匀，异质性 CI lower −0.0199。该 claim 已 DELETED。
- **Per-set** P5b `MECHANISM_NOT_ESTABLISHED`：Set B 字面负对照（permuted CI upper ≤ 0）不满足（+0.0204）。已在 overall combined 中诚实处理（7.6% 残差 + 可解释原因），但个体 Set B 永不改判。
- **Overall** P5 gate 通过合取联合聚合 = `MECHANISM_EVIDENCE_PASS`，4 条 caveats 全部永久附着在机制叙事中。
- P2 `signed-delta MAE` 为负（−6.5%）但 CRPS 主估计为正：已作为强制 secondary 如实报告（P2 handoff）。
- M3SARS 数据集噪声大（经验残差 SD 1.37，95% 覆盖率 0.781）：作为校准诊断如实报告，不改冻结 scale。
